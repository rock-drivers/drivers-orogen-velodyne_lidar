# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(velodyne_lidarTaskLib)
ADD_LIBRARY(${VELODYNE_LIDAR_TASKLIB_NAME} SHARED 
    ${VELODYNE_LIDAR_TASKLIB_SOURCES})

set(CMAKE_MODULE_PATH ${PROJECT_SOURCE_DIR})
find_package(GDAL REQUIRED)
add_definitions(${GDAL_CFLAGS})
include_directories(${GDAL_INCLUDE_DIRS})
link_directories(${GDAL_LIBRARY_DIRS})

add_dependencies(${VELODYNE_LIDAR_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${VELODYNE_LIDAR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${VELODYNE_LIDAR_TASKLIB_DEPENDENT_LIBRARIES}
    ${GDAL_LIBRARIES})
SET_TARGET_PROPERTIES(${VELODYNE_LIDAR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VELODYNE_LIDAR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VELODYNE_LIDAR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VELODYNE_LIDAR_TASKLIB_HEADERS}
    DESTINATION include/orocos/velodyne_lidar)

